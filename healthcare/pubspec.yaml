name: healthcare
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8
  
  # Firebase dependencies locked to a stable, compatible set to fix native build errors.
  firebase_core: 2.27.0
  firebase_auth: 4.17.8
  cloud_firestore: 4.15.8
  firebase_database: 10.4.1
  firebase_storage: 11.6.9

  curved_navigation_bar: ^1.0.3
  fl_chart: ^0.66.0
  image_picker: ^1.0.7
  intl: any
  provider: ^6.1.1
  syncfusion_flutter_gauges: ^31.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Ajout de la section assets pour l'illustration
  assets:
    - assets/doctor_illustration.jpg
